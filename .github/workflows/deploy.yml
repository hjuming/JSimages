name: Deploy Worker

on:
  push:
    branches:
      - main  # 或是你的主要分支名稱

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20' 
      
      # 安裝 wrangler
      - name: Install dependencies
        run: npm install

      # 步驟 1: 先部署 Worker (這會建立 Worker)
      - name: Deploy Worker
        run: npm run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      
      # 步驟 2: 再將密鑰設定到已建立的 Worker
      - name: Set Worker Secrets
        run: |
          echo ${{ secrets.USERNAME }} | npx wrangler secret put USERNAME
          echo ${{ secrets.PASSWORD }} | npx wrangler secret put PASSWORD
          echo ${{ secrets.DOMAIN }} | npx wrangler secret put DOMAIN
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
